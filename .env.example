DATABASE_URL=
DIRECT_URL=
SESSION_SECRET=
GEMINI_API_KEY=
# Stockage des photos hors base de données (Vercel → Storage → Blob).
# Sans ce jeton, les photos retombent en base64 dans la base : fonctionnel,
# mais très coûteux en transfert. À définir en local ET sur Vercel.
BLOB_READ_WRITE_TOKEN=